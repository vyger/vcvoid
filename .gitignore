.DS_Store
tools/droidcheck/build/
tools/droidcheck/vendor/
.superpowers/
build/
droid-manual-blue-7.pdf
plugin/build/
plugin/dist/
plugin/plugin.dylib
plugin/plugin.so
plugin/plugin.dll
# The engine is built from ../engine; the SDK writes its .o/.d objects here
# (the build/../engine/ path collapses out of build/). Purely generated.
plugin/engine/
.claude/scheduled_tasks.lock
docs/todo-uat.md
docs/superpowers/
docs/uat/results/
manual/images/
NOTES.md
# local-only rack-specific test patches
patches/e4-menus-full-rack.ini
patches/e4-menus-full-rack-m18.ini
# throwaway hardware-probe patches: written to answer one question against the
# real rack, then thrown away. The ANSWERS belong in code comments, goldens and
# the issue — never the scaffolding that produced them.
patches/tmp-*

# general per-session scratch space for this project (feedback dumps, repro
# material, throwaway notes). Never committed.
.claude/scratch/
