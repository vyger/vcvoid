# Tier-4 proof patch: E4 controller.
# Turning encoder 1 sweeps O1 (LED ring reflects the value automatically).
# Pushing the encoder's built-in button toggles O2.

[e4]

[encoder]
    encoder = E1.1
    output = O1
    color = 0.4

[button]
    button = B1.1
    output = O2
